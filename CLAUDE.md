# provider-app (`decatho_provider_app`) — Provider mobile app

React Native + Expo + NativeWind (Tailwind classes) + TypeScript. Source in `src/`.
Doctor / clinic-facing counterpart to `customer-app`.

## Figma — page "Provider app"

- File key: `rHKJH65oGtamnJM748MbJo`
- File URL: https://figma.com/design/rHKJH65oGtamnJM748MbJo/Docatho

The "Provider app" page node ID is not indexed yet (the page must be opened once in the
Figma desktop app to be exposed to the MCP). To use it:

1. Open the **Provider app** page in Figma desktop, or paste a Figma frame link here.
2. Ask me to record the page's node IDs — I'll add a jump table like the one in
   `customer-app/CLAUDE.md`.

Style output as **NativeWind / Tailwind** utilities to match the codebase.
